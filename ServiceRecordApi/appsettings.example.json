{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Postgres": "Host=${HOST};Port=${PORT};Username=${USERNAME};Password=${PASSWORD};Database=${DATABASE_NAME}"
  }
}
